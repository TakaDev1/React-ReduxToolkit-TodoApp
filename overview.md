## **課題**

Redux Toolkitを使って、Todoリストを管理するアプリを作成してください。
Todoは「追加」「削除」「完了切り替え」ができるようにします。

### **条件**

(1) TypeScriptを使用すること
(2) `createSlice`を使って`todos`を管理する
(3) 各Todoは`id`, `text`, `completed`を持つ
(4) 完了状態を切り替えるボタンを設置する
(5) TailwindCSSで最低限のスタイルを適用する（中央寄せ・余白）
